EDIT_FIRST_RULE = """
IMPORTANT AGENT RULES:

1. ALWAYS scan project before writing files
2. If a relevant file exists, EDIT IT instead of creating a new one
3. Only create new files if explicitly requested
4. Never duplicate existing functionality
5. Prefer modifying radar.py, main.py, or existing scripts
6. Treat project as an existing codebase, not blank canvas
"""
